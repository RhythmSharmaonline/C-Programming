#include <stdio.h> 

int main()
{
    //Variables: In programming, a variable is a named container in memory that stores a value that can change. Variables are essential for writing efficient code because they allow developers to label and store data in a program.
    // Basically Variables are containers which stores our data in Memory.
    int x; //Declaring a variable

    x=373; //Assigning a value to a variable (Initializing a variable)

    int y=98595; //Declaring and initializing a variable

    printf("%d", x); //Printing the value of a variable
    return 0;
}